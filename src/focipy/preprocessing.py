"""Image preprocessing module for FociPy.

Functions for intensity normalization, noise reduction,
and background subtraction of fluorescence microscopy images.
"""

# TODO: Implement the following:
# - normalize_intensity(image, low_percentile, high_percentile)
# - apply_gaussian_filter(image, sigma)
# - apply_median_filter(image, radius)
# - subtract_background(image, radius)  # top-hat transform
