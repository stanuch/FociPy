"""Main analysis pipeline for FociPy.

Orchestrates the full workflow: image loading, preprocessing,
nuclear segmentation, foci detection, quantification, and export.
"""

# TODO: Implement the following:
# - run_pipeline(input_dir, output_dir, config) -> DataFrame
# - process_single_image(image_path, config) -> dict
# - FociPipeline class (stateful, configurable)
