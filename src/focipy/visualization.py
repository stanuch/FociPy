"""Visualization module for FociPy.

Functions for creating overlay images, publication-ready plots,
and interactive visualizations of analysis results.
"""

# TODO: Implement the following:
# - overlay_segmentation(image, labeled_mask) -> RGB image
# - overlay_foci(image, foci_coords, nucleus_mask) -> RGB image
# - plot_dose_response(df, dose_column, count_column) -> Figure
# - plot_bland_altman(automated, manual) -> Figure
# - plot_correlation(automated, manual) -> Figure
