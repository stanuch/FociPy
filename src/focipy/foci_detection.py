"""XRCC1 foci detection module for FociPy.

Algorithms for detecting and localizing fluorescent foci
(protein accumulation sites) within segmented nuclei.
"""

# TODO: Implement the following:
# - detect_foci_dog(image, sigma_low, sigma_high, threshold, nucleus_mask) -> coords
# - detect_foci_log(image, min_sigma, max_sigma, threshold) -> coords
# - detect_foci_local_max(image, min_distance, threshold_abs, labels) -> coords
# - assign_foci_to_nuclei(foci_coords, labeled_nuclei) -> dict
