"""Reporting module for FociPy.

Handles all file output: experiment metadata (JSON),
per-cell kinetics data (CSV), summary tables,
and quality-control images. Ensures full audit trail
with source file hashes and pipeline parameters.
"""

# TODO: Implement the following:
# - create_output_dir(base_dir, experiment_name) -> Path
# - save_experiment_metadata(output_dir, metadata, config, source_hash) -> Path
# - save_kinetics_csv(output_dir, df, cell_id) -> Path
# - save_summary_csv(output_dir, summary_df) -> Path
# - save_cell_stats_json(output_dir, stats, cell_id) -> Path
